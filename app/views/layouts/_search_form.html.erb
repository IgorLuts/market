<%= search_form_for Product.ransack(params[:q]), html: {class: "navbar-form navbar-right" } do |f| %>
  <div class="form-group">
    <div class="input-group">
      <%= f.search_field :title_cont, placeholder: "Поиск", class: "form-control" %>
      <span class="input-group-btn">
        <button type="submit" class="btn"><span class="fui-search"></span></button>
      </span> 
    </div> 
  </div>
<% end %>
